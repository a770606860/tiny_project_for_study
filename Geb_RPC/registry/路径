请求体与响应体均使用JSON编码

注册中心服务端http路径：
/services: 获取服务的路径
请求头字段: name头字段为服务名称，
响应：响应体为地址列表，类型[]string
/heartbeat:  客户端发送心跳到服务中心
请求头字段: id头字段为发送心跳的客户端
/register:  注册客户端，
请求头字段: name客户端名称，addr客户端地址，tick心跳间隔单位s，lAddr监听更新的客户端地址
响应头：id为分配给该客户端的唯一id
/resign:    注销客户端，
请求头字段: id客户端id

注册中心客户端http路径：
/update: 注册中心服务端发送更新信息的地址，当某个服务被注册或者注销时会将该服务所有存活的实例发送到监听该服务的客户端
请求头字段: name服务名称
请求体: 服务地址列表，类型[]string